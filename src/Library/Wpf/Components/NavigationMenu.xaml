<UserControl x:Class="Library.Wpf.Components.NavigationMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cc="clr-namespace:Common.Wpf.Controls;assembly=Common.Wpf.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Library.Core.ViewModels;assembly=Library.Core"
             x:Name="root"
             d:Background="{StaticResource appBackground}"
             d:DataContext="{d:DesignInstance Type=vm:NavigationViewModel}"
             d:DesignHeight="90"
             d:DesignWidth="600"
             mc:Ignorable="d">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Common.Wpf.Controls;component/Themes/HamburgerMenu.xaml"/>
      </ResourceDictionary.MergedDictionaries>
      <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Border Grid.Row="0"
            Padding="5,4,5,8"
            Background="{DynamicResource navBackground}">
      <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <CheckBox x:Name="cbToggleMenu"
                  Grid.Column="0"
                  IsChecked="{Binding IsMenuOpen}"
                  Style="{StaticResource controlToggleMenuStyle}"/>
        <DockPanel Grid.Column="1" Margin="20,0,0,0">
          <TextBlock DockPanel.Dock="Left"
                     FontSize="16"
                     Foreground="White"
                     Text="{Binding MenuTitle}"/>
        </DockPanel>
      </Grid>
    </Border>
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <cc:HamburgerMenu Grid.Column="0" IsOpen="{Binding IsChecked, ElementName=cbToggleMenu}">
        <cc:HamburgerMenu.Content>
          <StackPanel Margin="0,10">
            <cc:HamburgerMenuItem Command="{Binding NavigateAboutCommand}">
              <TextBlock Text="About"/>
            </cc:HamburgerMenuItem>
            <cc:HamburgerMenuItem Command="{Binding NavigateAuthorsCommand}">
              <TextBlock Text="Authors"/>
            </cc:HamburgerMenuItem>
            <cc:HamburgerMenuItem Command="{Binding NavigateSectionsCommand}">
              <TextBlock Text="Sections"/>
            </cc:HamburgerMenuItem>
            <cc:HamburgerMenuItem Command="{Binding NavigateMergeCommand}" Visibility="{Binding HasMerges, Converter={StaticResource BooleanToVisibilityConverter}}">
              <TextBlock Text="Merge"/>
            </cc:HamburgerMenuItem>
            <cc:HamburgerMenuItem Command="{Binding SaveChangesCommand}" Visibility="{Binding HasChanges, Converter={StaticResource BooleanToVisibilityConverter}}">
              <TextBlock Text="Save"/>
            </cc:HamburgerMenuItem>
          </StackPanel>
        </cc:HamburgerMenu.Content>
      </cc:HamburgerMenu>

      <ContentControl Grid.Column="1"
                      Margin="5,0,5,5"
                      Content="{Binding ContentView, ElementName=root}"
                      Validation.ErrorTemplate="{x:Null}"/>
    </Grid>
  </Grid>

</UserControl>